!!!
%html
  = render 'shared/head'
  %body
    - if @item || @is_error_page
      = render 'shared/nav_show'
    - else
      = render 'shared/nav'
    .page-content
      = yield
    = render 'shared/footer'
